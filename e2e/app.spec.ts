import{test,expect}from'playwright/test';
test('enter binary, hash file, save and persist landmark',async({page})=>{await page.goto('/explore');await page.getByLabel('Binary input').fill('101');await expect(page.getByTestId('key-output')).toContainText('12');await page.getByRole('button',{name:/Move camera/}).click();await page.setInputFiles('[data-testid="file-input"]',{name:'sample.txt',mimeType:'text/plain',buffer:Buffer.from('hello atlas')});await expect(page.getByText(/SHA-256/)).toBeVisible();await page.getByTestId('save-landmark').click();await expect(page.getByText(/Landmark 1 key/)).toBeVisible();await page.reload();await expect(page.getByText(/Landmark 1 key/)).toBeVisible()});
