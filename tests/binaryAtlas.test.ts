import{describe,it,expect}from'vitest';import{binaryToKey,getChildKeys,getParentKey,getSharedPrefixLength,keyToBinary,keyToLocation,binaryToLocation}from'../src/lib/binaryAtlas';
describe('Binary Atlas math engine',()=>{it('maps required key examples',()=>{const cases:Array<[bigint,number,string]>=[[1n,1,'0'],[2n,1,'1'],[3n,2,'00'],[4n,2,'01'],[5n,2,'10'],[6n,2,'11'],[7n,3,'000'],[12n,3,'101'],[14n,3,'111'],[15n,4,'0000']];for(const[k,l,b]of cases){const loc=keyToLocation(k);expect(loc.level).toBe(l);expect(loc.binary).toBe(b)}});it('round trips keys and binaries',()=>{for(const k of[1n,2n,12n,255n,900719925474099112345n])expect(binaryToKey(keyToBinary(k))).toBe(k);for(const b of['0','1','101','0000','1'.repeat(80)])expect(keyToBinary(binaryToKey(b))).toBe(b)});it('supports a 256-bit value',()=>{const b='1'.repeat(256);const loc=binaryToLocation(b);expect(loc.level).toBe(256);expect(loc.key>2n**256n).toBe(true)});it('computes parent child relationships',()=>{expect(getParentKey(12n)).toBe(5n);expect(getChildKeys(5n)).toEqual([11n,12n])});it('computes shared prefixes',()=>{expect(getSharedPrefixLength('10110','10100')).toBe(3);expect(getSharedPrefixLength('0','1')).toBe(0)});it('rejects invalid values',()=>{expect(()=>keyToBinary(0n)).toThrow();expect(()=>binaryToKey('102')).toThrow();expect(()=>binaryToKey('')).toThrow()})});
